Add array type support to p24p (Phase 1). The lexer already tokenizes the array keyword (TOK_ARRAY=44). Implement: (1) array[low..high] of BaseType syntax in type declarations, (2) element access and assignment via index expressions, (3) p-code emission: contiguous allocation for globals/locals, address computation (base + index * element_size), loadg/storeg or addrg+load/store, (4) byte-level access via loadb/storeb for future array-of-char. This is BLOCKING for sw-cor24-basic (program store, input buffer, token buffer, string pool, variable table, GOSUB/FOR stacks). GitHub issue #2.
Add export code generation for unit compilation. When compiling a 'unit' (not a 'program'), emit .export directives for all public procedures and functions. Add a proc_is_exported[] flag to the procedure table. In a unit, all top-level procedures are exported by default (matching Turbo Pascal semantics where interface-section declarations are public). Emit .unit <name>, .export entries, procedure bodies, .endunit. Test: compile a simple unit, verify .spc output has correct .export directives.
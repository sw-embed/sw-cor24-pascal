End-to-end test of multi-unit compilation and linking. Create a test case: a simple unit (e.g., math_utils with add/multiply functions) and a main program that uses it. Compile the unit, compile the main program, link with pl24r or assemble separately and combine with p24-load. Run on PVM and verify correct output. Update build scripts (run-pascal.sh, justfile) to support multi-unit workflows. Add test to test-all.sh.
Add user-defined procedure and function support to p24p (Phase 1). The lexer already tokenizes procedure/function keywords (TOK_PROCEDURE=40, TOK_FUNCTION=41). Implement: (1) procedure declarations with value parameters and local vars, (2) function declarations with return type and name-assignment for return value, (3) p-code emission: .proc directives with enter/leave, call/ret, loadl/storel for params and locals, (4) procedure calls as statements, function calls in expressions. This is BLOCKING for sw-cor24-basic. GitHub issue #1.
Implement Phase 0 code generation (.spc p-code emission). The parser currently parses Pascal source but does not yet emit .spc output. Add code generation to parser.c (or a separate codegen.c) that walks the parsed program and emits p-code assembler instructions targeting the pv24a VM. Implement emission for: program prologue/epilogue, integer and boolean literals (LIT), variable load/store (LOD/STO), arithmetic operators (OPR ADD/SUB/MUL/DIV/MOD), comparison operators (OPR EQ/NEQ/LT/GTE/LTE/GT), boolean operators (OPR AND/OR/NOT), if/then/else (JPC/JMP), while/do loops (JPC/JMP), writeln (CSP WRI/WRL), assignment statements. Use the pv24a p-code instruction set from docs/design.md. Create a test that compiles a small Pascal program to .spc, assembles it with pasm, and runs it on pv24a. Verify the output is correct.
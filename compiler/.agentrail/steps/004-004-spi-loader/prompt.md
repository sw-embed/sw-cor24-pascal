Add .spi interface file loading to the compiler. When 'uses <unitname>' is encountered (and unitname is not 'hardware' or 'units'), search for <unitname>.spi in the current directory and a configurable search path. Parse the .spi file and register each exported procedure in the procedure table as an external (similar to how runtime builtins are registered). Set proc_is_user[] = 0 so they emit xcall. Emit .import <unitname> and .extern directives for all imported symbols. Test: compile a program that uses a unit, verify .spc output has correct .import and .extern directives.
Added SPI loader: load_spi_sections() parses ;--- SPI <name> --- delimited sections prepended to input, registers imported procs with full signatures (argc, has_ret, ret_type). Extended .export format to '.export <name> <argc> <has_ret> <ret_type>'. parse_uses_clause() recognizes SPI-loaded unit names, enables unit_mode, emits .import and .extern directives. xcall used for imported function calls. Fixed tc24r printf4 limitation by splitting printf calls. All 37 tests pass.
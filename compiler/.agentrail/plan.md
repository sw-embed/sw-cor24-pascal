Add user-defined unit support to p24p. Enable Pascal programs to define units with interface/implementation sections, export procedures, import other units via 'uses', and link multiple user units together. This extends the existing unit-mode infrastructure (runtime unit, pl24r linker) to support arbitrary user-defined units.
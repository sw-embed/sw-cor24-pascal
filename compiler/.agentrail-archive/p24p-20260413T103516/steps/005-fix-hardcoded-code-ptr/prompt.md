Fix bug: run-pascal.sh and test-all.sh use hardcoded code_ptr address 0x0A12 but pvm.s now assembles code_ptr at 0x0C57. Dynamically resolve code_ptr address at build time using: CODE_PTR_ADDR=$(cor24-run --run "$PVM" -e code_ptr --speed 0 -n 0 2>&1 | grep 'Entry point:' | sed 's/.*@ //'). Also increase the -n 5000000 compilation limit in run-pascal.sh line 27 (larger programs need ~20M+ instructions). GitHub issue #4.
Add char type and byte-level operations to p24p (Phase 1). Implement: (1) char variable declarations and char literals ('A'), (2) ord(ch) and chr(n) builtin functions, (3) char comparison and arithmetic via ord, (4) byte-level memory access via loadb/storeb opcodes, (5) array of char support. Important for sw-cor24-basic tokenizer (character classification, case conversion, keyword matching, digit parsing). GitHub issue #3.
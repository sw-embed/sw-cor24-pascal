Added unit compilation mode to p24p via 'uses units' directive. Emits .unit/.endunit, .import p24p_rt, .extern with arg counts, xcall for runtime calls, call for user procs. Added emit_rt_call() helper. Fixed static-link trampoline naming for pa24r main-at-offset-0 check. Also fixed pa24r to skip the check in unit mode. Switched build scripts from --terminal to -u. All 21 tests pass, unit-mode .spc assembles into valid v2 .p24.
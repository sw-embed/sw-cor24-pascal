Fix compiler hang on programs >4KB: switch all build scripts from piped --terminal to -u (preloaded UART input) to bypass cor24-run's ~4110-byte terminal input buffer limit. Update run-pascal.sh, test-all.sh, demo.sh, demo-led.sh, and justfile. Filter -u output: strip [UART RX] trace lines and 'UART output: ' prefix. Verify all 21 tests pass. Root cause: cor24-run --terminal mode silently drops piped stdin beyond ~4110 bytes; getchar() then hangs waiting for the lost EOT byte. Filed as sw-embed/cor24-rs#2.
Implemented Phase 0 recursive descent parser (parser.h, parser.c) with: symbol table using parallel arrays (workaround for tc24r struct array bugs), single-pass .spc emission, type checking, const folding, all Phase 0 grammar constructs. Fixed tc24r issues: no multiplication in array subscripts, no adjacent string literal concatenation, printf max 3 args. Test parses Countdown program and produces correct .spc matching the spec. Both lexer and parser tests pass on cor24-run.